import DB, { db } from '@/classes/DB'
import ServiceError from '@/classes/ServiceError'
import { parseJSON } from '@/libs/objects'
import type { ArticleModel } from './__model'

export default async function patchChangeNest({  })
{
  try
  {
    //
  }
  catch (_e: any)
  {
    //
  }
}
